import React from "react";
import {useCart} from 'react-use-cart';
const Cart= ()=>{
    const {items}=useCart();
    return(
        <div>
{console.warn()}
        </div>
    )
}
export default Cart;